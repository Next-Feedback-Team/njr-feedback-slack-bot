{
  "name": "njr-feedback-slack-bot",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "dev": "ts-node-dev --poll --respawn --transpile-only --files index.ts",
    "build": "tsc -p .",
    "start": "SET NODE_ENV=production & node dist/index.js"
  },
  "repository": "https://github.com/yutakobayashidev/njr-feedback-slack-bot",
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.12.2",
    "dotenv": "^16.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@types/node": "^18.14.0"
  }
}
